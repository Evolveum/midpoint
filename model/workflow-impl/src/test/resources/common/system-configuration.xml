<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
  ~ Copyright (C) 2010-2022 Evolveum and contributors
  ~
  ~ Licensed under the EUPL-1.2 or later.
  -->

<systemConfiguration oid="00000000-0000-0000-0000-000000000001" version="0"
        xmlns="http://midpoint.evolveum.com/xml/ns/public/common/common-3"
        xmlns:c="http://midpoint.evolveum.com/xml/ns/public/common/common-3">

    <name>SystemConfiguration</name>
    <logging>
        <rootLoggerAppender>File Appender</rootLoggerAppender>
        <rootLoggerLevel>INFO</rootLoggerLevel>
        <classLogger>
            <level>TRACE</level>
            <package>com.evolveum.midpoint.common.LoggingConfigurationManager</package>
        </classLogger>
        <appender xsi:type="c:FileAppenderConfigurationType" name="File Appender" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
            <pattern>%date [%thread] %-5level \(%logger{46}\): %message%n</pattern>
            <fileName>target/test.log</fileName>
            <append>true</append>
        </appender>
    </logging>
    <notificationConfiguration>
        <handler>
            <simpleWorkflowNotifier>
                <category>workflowProcessEvent</category>
                <recipientExpression>
                    <!-- Demonstration of parsed raw type of type Focus which is also acceptable here -->
                    <value xsi:type="UserType" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
                        <emailAddress>recipient@evolveum.com</emailAddress>
                    </value>
                </recipientExpression>
                <transport>dummy:simpleWorkflowNotifier-Processes</transport>
            </simpleWorkflowNotifier>
        </handler>
        <handler>
            <simpleWorkflowNotifier>
                <category>workItemEvent</category>
                <recipientExpression>
                    <value>recipient@evolveum.com</value>
                </recipientExpression>
                <transport>dummy:simpleWorkflowNotifier-WorkItems</transport>
            </simpleWorkflowNotifier>
        </handler>
        <handler>
            <simpleTaskNotifier>
                <expressionFilter>
                    <script>
                        <!-- MID-4121 @formatter:off -->
                        <code>
                            import com.evolveum.midpoint.wf.impl.assignments.TestAssignmentsAdvanced

                            try {
                                return event.isSuccess()
                            } catch (Exception e) {
                                TestAssignmentsAdvanced.exception = e
                                throw e
                            }
                        </code>
                        <!-- @formatter:on -->
                    </script>
                </expressionFilter>
            </simpleTaskNotifier>
        </handler>
        <handler>
            <simpleWorkflowNotifier>
                <category>workflowProcessEvent</category>
                <operation>delete</operation> <!-- should be more information here -->
                <bodyExpression>
                    <script>
                        <language>http://midpoint.evolveum.com/xml/ns/public/expression/language#safe-velocity</language>
                        <code> <!-- These are expressions from the notifications training -->
                            [00: $event.statusAsText]

                            [10: $event.requesterDisplayName]
                            [11: $event.requesterName]

                            [50: $event.caseName]
                        </code>
                    </script>
                </bodyExpression>
                <transport>dummy:workflow-process-safe</transport>
            </simpleWorkflowNotifier>
            <simpleWorkflowNotifier>
                <category>workItemLifecycleEvent</category>
                <operation>delete</operation> <!-- we need operationKind to be non-null, so we need completion notification -->
                <bodyExpression>
                    <script>
                        <language>http://midpoint.evolveum.com/xml/ns/public/expression/language#safe-velocity</language>
                        <code> <!-- These are expressions from the notifications training -->
                            [00: $event.statusAsText]
                            [01: $event.getOperationKind()]

                            [10: $event.requesterDisplayName]
                            [11: $event.requesterName]

                            [20: $event.requesteeDisplayName]
                            [21: $event.requesteeName]

                            [50: $event.caseName]

                            [60: $event.workItemUrl]
                            [61: $event.getWorkItem().getDeadline()]
                            [62: $event.getCause().getType()]

                            Complex scenario:

                            [event: $event]

                            [approvalContext: $event.approvalContext]

                            [deltasToApprove: $event.approvalContext.deltasToApprove]

                            #set ($primaryDelta = $event.approvalContext.deltasToApprove.focusPrimaryDelta)
                            [primaryDelta: $primaryDelta]

                            #set ($changeType = ${event.getApprovalContext().getDeltasToApprove().getFocusPrimaryDelta().getChangeType()})
                            [changeType: $changeType]

                            #set ($itemDeltas = ${event.getApprovalContext().getDeltasToApprove().getFocusPrimaryDelta().getItemDelta()})
                            [itemDeltas: $itemDeltas]

                            #foreach ($itemDelta in $itemDeltas)

                              [itemDelta: $itemDelta]
                              [values size: $itemDelta.getValue().size()]

                              #foreach ($itemDeltaValue in $itemDelta.getValue())

                                [itemDeltaValue: $itemDeltaValue]

                                #set ($modificationType = $itemDelta.getModificationType())

                                [modificationType: $modificationType]
                                [path: $itemDelta.getPath()]

                                #if ($itemDelta.getPath() == "assignment")

                                  #set ($assignmentRaw = $itemDeltaValue)
                                  [assignmentRaw: $assignmentRaw]

                                  #set ($assignment = $assignmentRaw.getValue())
                                  [assignment: $assignment]

                                  [targetRef: $assignment.targetRef]
                                  [targetRef type: $assignment.targetRef.type]

                                  #set ($targetTypeShort = $assignment.getTargetRef().getType().getLocalPart())

                                  [targetTypeShort: $targetTypeShort]

                                  #set ($obj = $midpoint.resolveReferenceIfExists(${assignment.getTargetRef()}))

                                  [obj: $obj] (target)
                                  [obj.name: $obj.getName()]
                                  [obj.displayName: $obj.getDisplayName()]

                                #else
                                  \$error (this will be reported!)
                                #end
                              #end
                            #end
                        </code>
                    </script>
                </bodyExpression>
                <transport>dummy:work-item-safe</transport>
            </simpleWorkflowNotifier>
        </handler>
    </notificationConfiguration>
    <workflowConfiguration>
        <approverCommentsFormatting>
            <value>
                <script>
                    <code>performer.name + ' :: ' + output.comment</code>
                </script>
            </value>
            <condition>
                <script>
                    <code>output.comment != null &amp;&amp; !output.comment.isEmpty()</code>
                </script>
            </condition>
        </approverCommentsFormatting>
        <defaultExpandRolesOnPreview>true</defaultExpandRolesOnPreview>
        <primaryChangeProcessor>
        </primaryChangeProcessor>
    </workflowConfiguration>
    <infrastructure>
        <publicHttpUrlPattern>http://evolveum.com/testing</publicHttpUrlPattern> <!-- to check the notifications -->
    </infrastructure>
</systemConfiguration>
